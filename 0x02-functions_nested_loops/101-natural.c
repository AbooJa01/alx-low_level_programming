#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * AbooJa01
 * Return: Null
**/

int main(void)

{
	int a = 0;
	int sum = 0;

	while (a < 1024)
	{
		if (c % 3 == 0 || c % 5 == 0)
		{
			sum += c;
		}
		c++;
	}
	printf("%i\n", sum);
	return (0);
}
